{
  "name": "meridian-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end browser tests for Meridian Components inventory dashboard",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
