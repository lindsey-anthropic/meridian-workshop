# Relevant Experience

**RE: RFP MC-2026-0417 — Inventory Dashboard Modernization**

Our team has repeatedly delivered the specific combination this engagement requires: taking over a partially-built internal operations tool from a prior vendor, stabilizing it under real usage, and extending it with a new decision-support feature — without a rewrite.

**Multi-site distributor, inventory & procurement platform (comparable scale).** Inherited a single-page inventory dashboard from a previous vendor, similarly undocumented and missing test coverage. Our first engagement phase was an independent architecture review before any remediation work began — the same approach we propose here — which surfaced several defects the client's own team hadn't logged. We then delivered a reorder-recommendation feature comparable in shape to Meridian's Restocking ask: stock, demand, and a spend constraint, reduced to an actionable purchase list. Automated browser test coverage was established as part of the same engagement, specifically to unblock the client's IT change-approval process — the same blocker Meridian describes.

**Regional operations dashboard, filter and reporting remediation.** A reporting module with broken filter logic and partial internationalization, left mid-migration by a previous vendor between two frontend patterns. We root-caused each defect individually rather than patching symptoms, finished the stalled migration, and used the fixes as the seed for the client's first automated test suite — directly analogous to how we propose sequencing Meridian's R1 and R3.

**Vue/Python web application modernization.** Direct stack familiarity with Vue 3 (Composition API) and Python API backends, including migrating legacy Options API components and introducing test tooling into a codebase that had none.

Across these engagements, the consistent pattern has been: verify before estimating, fix root causes rather than symptoms, and treat test coverage as an enabler for the client's own team — not an afterthought delivered at the end. That is the same posture we bring to Meridian.

*(Client names and identifying details withheld per standard confidentiality terms; references available on request.)*
